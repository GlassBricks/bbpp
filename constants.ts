export const PREFIX = "bbpp:"

export const Prototypes = {
  bpHandlingChest: PREFIX + "bp-handling-chest",
  bpCenterMarker: PREFIX + "bp-center-marker",
}

export const Surfaces = {
  bpHandlingSurface: PREFIX + "blueprint-handling",
}

export const Defaults = {
  layerSize: {
    x: 10000,
    y: 10000,
  },
}
